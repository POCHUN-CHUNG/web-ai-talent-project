# 04 · 行為層

> 本檔為 `SPEC.md` 的子文件。閱讀前必須先讀 `SPEC.md` 的 §0 協議層與 §0.3 詞彙表。
> 文件版本：1.4.0 ｜ 最後更新：2026-09-21

本層定義量化演算法、問卷轉換規則、業務驗證、錯誤處理與介面規格。實作功能邏輯時必讀。

---

## 4.1 核心演算法

本節的每一條公式都是**唯一實作依據**。所有指標由後端算定，AI 不參與任何計算。
數值驗證向量見 `spec/appendix/C-fixtures.md`，比對標準為相對誤差 < 1e-6（P-20）。
**每條公式的文獻出處見 `spec/appendix/D-references.md` §D.2**；該附錄同時標明哪些門檻是本專題自訂、不具官方依據。

### 4.1.0 共同前置

**步驟 1：彙總部位與權重**

依 `holding_lots` 以 `symbol` 分組：

$$Q_i=\sum_k q_{ik},\qquad V_i=Q_i\cdot P_i,\qquad w_i=\frac{V_i}{\sum_{j=1}^{N}V_j}$$

$P_i$ 為該檔在**共同期間最後一個交易日**的 `adj_close`。$\sum w_i = 1$。
**買進日期不參與權重計算**（FR-26）。這是「固定目前權重的歷史模擬」，是刻意的口徑選擇。

**步驟 2：決定共同期間**

1. 目標區間為 `[今日 − requestedYears 年, 今日]`。
2. 對每一檔持股取其在目標區間內的 `trade_date` 集合。
3. 共同期間 = 所有持股交易日集合的**交集**，再與基準 `IR0001` 的交易日集合取交集。
4. `startDate` 與 `endDate` 為交集的最小與最大值，`tradingDays` 為交集大小。
5. 交集為空或大小 < 2 → `INSUFFICIENT_PRICE_DATA`。
6. 若 `effectiveYears < requestedYears`，將造成限縮的代號（其最早交易日 > 目標起點者）列入 `limitedBy`。

取交集而非聯集的理由：所有指標必須建立在同一組交易日上，否則共變異數矩陣的各元素來自不同樣本，數學上不成立。

**步驟 3：報酬序列**

$$R_{i,t}=\frac{P_{i,t}-P_{i,t-1}}{P_{i,t-1}}$$

$n = \text{tradingDays} - 1$ 為有效日報酬筆數。價格一律取 `adj_close`。

**步驟 4：組合日報酬與基準日報酬**

$$R_{p,t}=\sum_{i=1}^{N}w_i R_{i,t},\qquad R_{m,t}=\frac{M_t-M_{t-1}}{M_{t-1}}$$

$M_t$ 為 `IR0001` 的當日值。

**步驟 4b：零變異判定**

凡需要判斷「標準差為零」「變異數為零」的地方，**一律以 $\sigma < 10^{-12}$ 判定，不得用 `== 0`**。

理由不是理論而是浮點數的實際行為：常數序列的樣本標準差**不會剛好等於 0**。以 250 筆固定值 0.0005 為例，`np.std(x, ddof=1)` 回傳 $1.09\times10^{-19}$ 而非 0。用 `== 0` 判定會整條路徑失效，接著以該值當分母，產生「相關係數 0.0」這種看起來合理、實際上毫無意義的結果，而且**不會拋任何例外**。

適用位置：Sharpe 的分母 $\sigma_p$、Sortino 的分母 $DD_{\text{annual}}$、Beta 的分母 $\text{Var}(R_m)$、相關係數的分母 $\sigma_i\sigma_j$、RC 的分母 $\sigma_p$、偏態與峰度的標準化分母 $s$。

門檻取 $10^{-12}$ 的依據：日報酬的典型量級為 $10^{-2}$，門檻低於它 10 個數量級，不會誤判任何真實的低波動序列。

**步驟 5：無風險利率**

$$R_f=\frac{r_{\text{taiwan}}+r_{\text{tcb}}+r_{\text{land}}+r_{\text{huanan}}+r_{\text{first}}}{5\times 100}$$

資料表欄位以百分比儲存（`1.690` 代表 1.690%），故除以 100 轉為小數。

$$R_{f,\text{daily}}=(1+R_f)^{1/252}-1,\qquad MAR=R_f$$

**禁止**使用 $R_f/252$ 的簡單除法。兩者在 $R_f \approx 1.7\%$ 時差異雖小，但複利換算才與幾何年化的報酬定義一致。

### 4.1.1 年化報酬率

$$R_{p,\text{annual}}=\left(\prod_{t=1}^{n}(1+R_{p,t})\right)^{252/n}-1$$

**禁止**改用 $\bar{R}_p \times 252$（算術年化）。兩者在波動大時可差 10% 以上，且算術年化無法對應實際累積報酬。

若任一 $(1+R_{p,t}) \le 0$（組合單日跌幅達 100%，實務不可能）→ 標為 `unavailable`，`reason` 為「報酬序列含非正累積因子」。

### 4.1.2 年化波動度 σp

$$\sigma_{\text{daily}}=\sqrt{\frac{1}{n-1}\sum_{t=1}^{n}(R_{p,t}-\bar{R}_p)^2},\qquad \sigma_p=\sigma_{\text{daily}}\sqrt{252}$$

**樣本標準差，`ddof=1`。** NumPy 的 `np.std` 預設 `ddof=0`（母體），直接呼叫會得到偏小的值。必須寫 `np.std(x, ddof=1)`。

| 邊界 | 處理 |
| --- | --- |
| $n < 2$ | `unavailable`，`reason`「有效日報酬不足」 |
| $\sigma_p = 0$ | 值為 0，`available`。但 Sharpe 因分母為零而 `unavailable` |

### 4.1.3 年化下行波動度 DD

$$DD_{\text{daily}}=\sqrt{\frac{1}{n}\sum_{t=1}^{n}\min\left(R_{p,t}-R_{f,\text{daily}},\,0\right)^2},\qquad DD_{\text{annual}}=DD_{\text{daily}}\sqrt{252}$$

三個容易寫錯的點：

1. **分母是 $n$，不是「低於門檻的天數」。** 這是下行偏離（downside deviation）的定義，不是「只取負報酬樣本再求標準差」。
2. **門檻是 $R_{f,\text{daily}}$，不是 0。** 本版 $MAR = R_f$（D-10），原指標說明書的 $\min(R_t, 0)$ 公式已作廢。
3. **不減平均數。** 與標準差不同，下行偏離是相對於門檻，不是相對於樣本平均。

| 邊界 | 處理 |
| --- | --- |
| 所有 $R_{p,t} > R_{f,\text{daily}}$ | $DD = 0$，`available`；Sortino 因分母為零而 `unavailable` |
| $n < 2$ | `unavailable` |

### 4.1.4 Beta 與 R²

$$\beta_p=\frac{\text{Cov}(R_p,R_m)}{\text{Var}(R_m)},\qquad R^2=\text{Corr}(R_p,R_m)^2$$

共變異數與變異數皆為**樣本值（`ddof=1`）**。在單因子含截距的迴歸下，$R^2$ 等於相關係數平方，不需另跑最小平方法。

| 邊界 | 處理 |
| --- | --- |
| $\text{Var}(R_m)=0$ | 兩者皆 `unavailable`，`reason`「基準報酬無變異」 |
| 基準在共同期間無資料 | 整個分析回 `BENCHMARK_UNAVAILABLE`（步驟 2 已取交集，正常不會走到這裡） |
| $n < 2$ | 兩者皆 `unavailable` |

**禁止**以 Beta 推算未來跌幅。這條寫在 AI Prompt 裡，後端也不得提供任何此類衍生欄位。

### 4.1.5 最大回撤 MDD

$$V_t=\prod_{\tau=1}^{t}(1+R_{p,\tau}),\qquad \text{Peak}_t=\max_{\tau\le t}V_\tau,\qquad DD_t=\frac{V_t-\text{Peak}_t}{\text{Peak}_t}$$

$$MDD=\min_t DD_t$$

$V_0 = 1$。$\text{Peak}_t$ 的範圍包含 $V_0$，因此 $MDD \le 0$ 恆成立。

**回傳值保留負號**（例 `-0.2837`）。前端顯示時才轉為「−28.37%」。契約中留負號是為了讓大小比較不會因符號而顛倒。

同時輸出回撤走勢圖所需的序列：`[{date, navIndex, drawdown}]`，其中 `navIndex` 為 $V_t$、`drawdown` 為 $DD_t$。

### 4.1.6 95% 單日預期短缺 ES95

$$q_{0.05}=\text{Quantile}_{5\%}(R_p),\qquad ES_{95}=-\mathbb{E}\left[R_p\mid R_p\le q_{0.05}\right]$$

**分位數採線性內插**（NumPy 預設 `method="linear"`，等同 R 的 type 7）。分位數估計方法有九種，不同方法在小樣本下結果明顯不同，必須指定。

| 邊界 | 處理 |
| --- | --- |
| 尾端樣本數 | 因資料重值，實際落在尾端的筆數可能不等於 $n \times 5\%$。輸出 `tailCount` 供圖說使用 |
| $ES_{95} < 0$ | 代表最差 5% 交易日的平均仍為正報酬。**保留負值**，不改為 0，不宣稱「不會虧」 |
| $n < 20$ | `available`，但 `dataQuality.notes` 加入「尾端樣本不足 20 筆，ES 估計不穩定」 |

### 4.1.7 樣本偏態係數 G1

$$G_1=\frac{n}{(n-1)(n-2)}\sum_{t=1}^{n}\left(\frac{R_{p,t}-\bar{R}_p}{s}\right)^3$$

$s$ 為樣本標準差（`ddof=1`）。

**禁止使用 `scipy.stats.skew`。** 它預設回傳母體偏態（$b_1$，分母為 $n$），與本式的 $G_1$ 不同。兩者比值為 $\sqrt{n(n-1)}/(n-2)$，在 $n=1000$ 時差異約 0.15%，看起來很小，但會讓黃金向量比對失敗，且與文獻引用的定義不一致。本專案**不安裝 scipy**（`spec/02-system.md` §2.1）。

| 邊界 | 處理 |
| --- | --- |
| $n < 3$ | `unavailable`（分母為零） |
| $s = 0$ | `unavailable`，`reason`「報酬無變異」 |

### 4.1.8 樣本超額峰度 G2

$$G_2=\frac{n(n+1)}{(n-1)(n-2)(n-3)}\sum_{t=1}^{n}\left(\frac{R_{p,t}-\bar{R}_p}{s}\right)^4-\frac{3(n-1)^2}{(n-2)(n-3)}$$

**超額峰度，常態分布的參考值為 0**，不是 Pearson 峰度的 3。

| 邊界 | 處理 |
| --- | --- |
| $n < 4$ | `unavailable` |
| $s = 0$ | `unavailable` |

### 4.1.9 HHI 與有效持股檔數

$$HHI=\sum_{i=1}^{N}w_i^2,\qquad N_{\text{eff}}=\frac{1}{HHI}$$

$N_{\text{eff}}$ **由前端計算**，不列入 `metrics`，不送給 AI（D2 §八）。

$N=1$ 時 $HHI=1$、$N_{\text{eff}}=1$，皆為合法值。

### 4.1.10 風險貢獻度 RC 與 PCR

以**年化共變異數矩陣** $\Sigma = \Sigma_{\text{daily}} \times 252$（樣本共變異數，`ddof=1`）計算：

$$\sigma_p=\sqrt{w^\top\Sigma w},\qquad RC_i=w_i\frac{(\Sigma w)_i}{\sigma_p},\qquad PCR_i=\frac{RC_i}{\sigma_p}$$

恆等式 $\sum_i RC_i = \sigma_p$ 與 $\sum_i PCR_i = 1$ 必須成立，實作後以此自我驗證（容差 1e-9）。

用年化矩陣而非日矩陣，是為了讓這裡算出的 $\sigma_p$ 與 §4.1.2 的年化波動度**在數值上一致**。兩者理論上相等；若相差超過 1e-9，代表權重或共變異數的來源不一致，應直接拋錯而非取其一。

| 邊界 | 處理 |
| --- | --- |
| $\sigma_p = 0$ | 全部 `rc` 與 `pcr` 為 `null`，不硬算，不做來源排名 |
| $RC_i < 0$ | **合法**。代表該檔在本次共變異數結構下有抵銷效果。**不得改為 0、不得取絕對值**。其他持股的 $PCR$ 因此可能超過 100% |
| $N=1$ | $RC_1=\sigma_p$、$PCR_1=1$ |

### 4.1.11 相關係數矩陣

$$\rho_{ij}=\frac{\text{Cov}(R_i,R_j)}{\sigma_i\sigma_j}$$

Pearson 相關係數，樣本值。輸出為對稱方陣，對角線為 1。

| 邊界 | 處理 |
| --- | --- |
| 任一檔 $\sigma_i=0$ | 該檔所有非對角線元素為 `null`，**不得填 0**（0 代表「無線性關聯」，與「無法計算」意義完全不同） |
| $N=1$ | 矩陣為 `[[1]]`，熱圖 `status` 為 `unavailable`，`reason`「只有一檔持股，沒有可比較的配對」 |

### 4.1.12 夏普比率與索丁諾比率

$$\text{Sharpe}=\frac{R_{p,\text{annual}}-R_f}{\sigma_p},\qquad \text{Sortino}=\frac{R_{p,\text{annual}}-MAR}{DD_{\text{annual}}}$$

本版 $MAR=R_f$（D-10），因此兩者分子相同、分母不同。

| 邊界 | 處理 |
| --- | --- |
| $\sigma_p=0$ | Sharpe `unavailable`，`reason`「總波動為零」 |
| $DD_{\text{annual}}=0$ | Sortino `unavailable`，`reason`「無低於無風險利率的交易日」 |
| 結果為 $\pm\infty$ 或 NaN | `unavailable`，**不得回傳無限大** |

**禁止**把「Sortino 數值較大」解讀為風險較低。兩者分母的定義不同，數值大小不可直接比較。

### 4.1.13 偏態分類與績效解讀順序

後端依 $G_1$ 判定分類，AI 只能引用結果，不得自訂門檻（D2 §5.4）。

| 條件 | `skewClass` | `performanceFocus` |
| --- | --- | --- |
| $G_1$ 為 `unavailable` 或 $n < 30$ | `undetermined` | `undetermined` |
| $\lvert G_1\rvert < 0.5$ | `near_symmetric` | `sharpe_primary` |
| $G_1 \ge 0.5$ | `positive_skew` | `both` |
| $G_1 \le -0.5$ | `negative_skew` | `sortino_primary` |

Sharpe 或 Sortino 任一為 `unavailable` 時，`performanceFocus` 一律覆寫為 `limited`，不論偏態分類為何。

0.5 這個門檻採統計學常見的經驗判準（$\lvert\text{skew}\rvert < 0.5$ 視為近似對稱）。`interpretation.ruleSource` 欄位固定填 `"本專題規則 v1.0：|G1| < 0.5 視為近似對稱"`，讓 AI 與使用者都知道這不是官方公式。

### 4.1.14 計算複雜度與效能

| 步驟 | 複雜度 | $N=50$、$T=2520$ 的量級 |
| --- | --- | --- |
| 報酬序列 | $O(NT)$ | 126,000 |
| 共變異數矩陣 | $O(N^2T)$ | 6,300,000 |
| $\Sigma w$ | $O(N^2)$ | 2,500 |
| 其餘純量指標 | $O(T)$ | 2,520 |

全部以 NumPy 向量化實作，實測應遠低於 §1.3 的 3 秒預算。**禁止**以 Python 迴圈逐日累加——不只是慢，浮點累加順序不同會讓結果無法重現。

### 4.1.15 禁止的實作方式總表

| 禁止 | 原因 |
| --- | --- |
| `np.std(x)` 不帶 `ddof=1` | 預設為母體標準差，系統性偏小 |
| `scipy.stats.skew` / `kurtosis` | 預設為母體動差，與 $G_1$、$G_2$ 定義不同 |
| 引入 `pandas` | 體積遠大於需求；且 `DataFrame.std()` 預設 `ddof=1`、`np.std()` 預設 `ddof=0`，混用兩者是經典錯誤來源 |
| $R_f/252$ | 與幾何年化不一致，應用 $(1+R_f)^{1/252}-1$ |
| $\bar{R}_p\times 252$ | 算術年化無法對應實際累積報酬 |
| $\min(R_t,0)$ 作為下行偏離 | 舊版公式，門檻應為 $R_{f,\text{daily}}$ |
| 以 `close` 代替 `adj_close` | 長期報酬與 Beta 會靜默失真 |
| 缺值填 0 | 0 是有意義的數值，與「無法計算」不同 |
| 取區間中點代替使用者的損失界線 | 使用者只表示了區間，沒表示精確值 |
| 以 `float` 儲存金額 | 一律 `Decimal` 或 `NUMERIC` |
| Python 迴圈逐日累加 | 慢，且浮點累加順序影響可重現性 |
| 以 `== 0` 判斷零變異 | 常數序列的樣本標準差是 $10^{-19}$ 量級而非 0，判定會失效且不報錯。一律用 $< 10^{-12}$（§4.1.0 步驟 4b） |

---

## 4.2 業務規則與驗證

### 4.2.1 輸入驗證

| 欄位／情境 | 規則 | 執行層 | 違反時的行為 |
| --- | --- | --- | --- |
| `username` | 1–32 字、僅英數 | 前端 + API + DB UNIQUE | 400 `INVALID_INPUT`；重複為 409 `USERNAME_TAKEN` |
| `password` | 1–128 字、僅英數（D-19，維持現況） | 前端 + API | 400 `INVALID_INPUT` |
| 投資組合 `name` | 1–30 字、同使用者不重複 | 前端 + API + DB UNIQUE | 409 `PORTFOLIO_NAME_TAKEN` |
| `symbol` | 符合 `^([1-9]\d{3}\|00\d{2,3}[A-Za-z]?)$` 且存在於 `stock_info` 且 `market ≠ '指數'` | 前端 + API + DB FK | 400 或 404 `NOT_FOUND` |
| `tradeDate` | `1990-01-01` ≤ 日期 ≤ 今日 | 前端 + API | 400 `INVALID_INPUT` |
| `quantity` | > 0，小數 4 位 | 前端 + API + DB CHECK | 400 |
| `unitCost` | > 0，小數 4 位 | 前端 + API + DB CHECK | 400 |
| `lookbackYears` | 整數 1–10 | 前端 + API | 400 |
| `overrides` | 只允許 `lossTolerance`、`investmentHorizon` 兩鍵 | API | 400，**不得忽略多餘的鍵** |
| 問卷 Q1–Q14 | 全部必填；選項值須在該題清單內 | 前端 + API | 400 |
| Q11 | 至少選一項；選 `K` 時不得再選其他；選 `J` 時 `q11Other` 必填且 ≤ 100 字 | 前端 + API | 400 |
| 組合數 | 每使用者 ≤ 20 | API | 422 `LIMIT_EXCEEDED` |
| 不同股票檔數 | 每組合 ≤ 50 | API | 422 |
| 單一股票買進筆數 | ≤ 100 | API | 422 |
| 單次報價寫入列數 | ≤ 5000 | API | 422 |

**三層都要做**的理由：前端擋是體驗，API 擋是安全，DB 擋是最後防線。只在前端驗證的規則等於沒有驗證。

### 4.2.2 問卷轉換規則

規則來源為柏鈞提供的《指標轉換與風險分析規則說明書》，全部為**固定規則式運算**，不含任何評分加總。
問卷構面的設計依據與「哪些規則是本專題自訂」的完整說明見 `spec/appendix/D-references.md` §D.3 與 §D.4。

#### 核心指標一：可接受損失區間（Q13 直接保留）

| Q13 | `lossTolerance` |
| --- | --- |
| A | 未滿5% |
| B | 5%～10% |
| C | 10%～20% |
| D | 20%～30% |
| E | 30%以上 |

**不得取中點。** Q13 = C 就是「10%～20%」，不是 15%。

#### 核心指標二：投資期限（Q7 直接保留）

| Q7 | `investmentHorizon` |
| --- | --- |
| A | 1年以內 |
| B | 1～3年 |
| C | 3～5年 |
| D | 5～10年 |
| E | 10年以上 |

#### 核心指標三：資金流動性需求（Q8 為主，Q4 為下限）

先由 Q8 得基礎等級，再由 Q4 施加最低限制，取兩者較高者：

| Q4＼Q8 | A 很可能大量提款 | B 可能提款 | C 有可能、有其他資金 | D 不太可能 | E 無預期提款 |
| --- | --- | --- | --- | --- | --- |
| A 不足1個月 | 極高 | 高 | 高 | 高 | 高 |
| B 1～3個月 | 極高 | 高 | 中等 | 中等 | 中等 |
| C 3～6個月 | 極高 | 高 | 中等 | 低 | 低 |
| D 6～12個月 | 極高 | 高 | 中等 | 低 | 低 |
| E 12個月以上 | 極高 | 高 | 中等 | 低 | 低 |

等級高低順序為：極高 > 高 > 中等 > 低。

此矩陣的用意是排除「沒有提款計畫 ＝ 沒有流動性需求」這個錯誤推論——完全沒有緊急預備金的人，突發支出時仍可能被迫賣股。

#### 核心指標四：財務風險承受能力（非補償式 MIN 規則）

先將三題各自分級：

| Q3 現金流 | 等級 | | Q4 預備金 | 等級 | | Q9 20%損失影響 | 等級 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| A 入不敷出 | 低 | | A 不足1個月 | 低 | | A 嚴重影響／需借款 | 低 |
| B 收支相抵 | 中等 | | B 1～3個月 | 中等 | | B 明顯財務壓力 | 低 |
| C 略有結餘 | 中等 | | C 3～6個月 | 中等 | | C 有一定影響 | 中等 |
| D 穩定結餘 | 高 | | D 6～12個月 | 高 | | D 影響不大 | 高 |
| E 結餘充足 | 高 | | E 12個月以上 | 高 | | E 幾乎無影響 | 高 |

$$\text{financialCapacity}=\min(\text{lv}(Q3),\ \text{lv}(Q4),\ \text{lv}(Q9))$$

| 條件 | 結果 |
| --- | --- |
| 任一為低 | 低 |
| 無低，至少一中等 | 中等 |
| 三者皆高 | 高 |

共 27 種組合，全部由此規則決定，**不需列舉**。這是**非補償式規則**：一個重大財務弱點不能被其他良好條件抵銷。

**AI 不得修改此等級**，亦不得因年齡、所得、經驗或損失意願高而推翻（D2 §四）。

#### 輔助資料

| 項目 | 來源 | 轉換 |
| --- | --- | --- |
| `cash_flow` | Q3 | 原始答案文字 |
| `emergency_reserve` | Q4 | 原始區間 |
| `withdrawal_need` | Q8 | 原始答案文字 |
| `investment_exposure` | Q5 | 原始區間 |
| `loss_impact_20pct` | Q9 | 五級：A 極低／B 低／C 中等／D 高／E 很高 |
| `market_decline_behavior` | Q14 | A 大幅退出／B 部分降低曝險／C 暫時觀察／D 檢視理由後持有／E 依策略重新配置／F 無法判斷 |
| `diversification_knowledge` | Q12 | 正確（C）／錯誤或不確定 |
| `age`、`income`、`investment_goal` | Q1、Q2、Q6 | 原始答案文字 |
| `investment_experience` | Q10 | 原始區間 |
| `product_experience` | Q11 | 無經驗／一般商品經驗／複雜商品經驗 |

**Q9 有兩套分級**（三級用於財務承受能力、五級用於輔助說明），**Q3 與 Q4 亦各有兩套**。識別碼必須分開命名，不得共用（`SPEC.md` §0.3）。

`product_experience` 的分類（D-20）：

| 類型 | Q11 選項 |
| --- | --- |
| 無經驗 | 只勾 K |
| 一般商品經驗 | A 定期存款、B 股票、C ETF、D 基金、E 債券、F 外幣、G 黃金原物料 |
| 複雜商品經驗 | H 期貨選擇權、I 結構型商品 |

選項 J「其他」**不參與分類**，其自由文字只作為 AI 的背景參考。同時符合一般與複雜時，歸為「複雜商品經驗」。

#### 短期財務韌性（Q3 + Q4 + Q8，四級非補償式）

| 答案 | 等級 |
| --- | --- |
| A | 低 |
| B | 中低 |
| C | 中高 |
| D 或 E | 高 |

三題各自分級後取最低：任一為低 → 低；無低但有中低 → 中低；全部至少中高但未全高 → 中高；三者皆高 → 高。

### 4.2.3 交叉分析

#### Finding 1：主要財務限制（priority 1）

以下 11 個條件逐一檢查，符合即加入標籤清單，可同時存在多項：

| 觸發條件 | 標籤 |
| --- | --- |
| Q3 = A | 現金流／償債能力限制 |
| Q4 = A | 緊急預備金嚴重不足 |
| Q4 = B | 緊急預備金偏低 |
| Q5 = A | 大部分金融資產暴露於投資市場 |
| Q7 = A | 投資期限過短 |
| Q8 = A | 一年內高度提款需求 |
| Q8 = B | 近期存在明顯資金需求 |
| Q9 = A | 重大損失可能危及基本生活 |
| Q9 = B | 重大損失可能造成明顯財務壓力 |
| 風險意願 > 能力 | 主觀風險意願高於客觀承受能力 |
| Q7／Q8 判定為衝突 | 投資期限與短期資金需求存在落差 |

無任何觸發時，此 finding 的 `statement` 為「目前未偵測到明顯的財務限制」，仍輸出（供 AI 說明），`factIds` 為空陣列。

#### Finding 2：風險意願－能力關係（priority 2）

Q13 先簡化為三級（僅供此比較使用）：A/B → 低、C → 中、D/E → 高。

| 財務能力＼風險意願 | 低 | 中 | 高 |
| --- | --- | --- | --- |
| 低 | 大致一致 | 意願高於能力 | 意願明顯高於能力 |
| 中等 | 意願低於能力 | 大致一致 | 意願高於能力 |
| 高 | 意願明顯低於能力 | 意願低於能力 | 大致一致 |

「意願低於能力」是合理偏好，**系統不得建議提高風險**。

#### Finding 3：投資期限－流動性一致性（priority 3）

| Q7＼Q8 | A | B | C | D | E |
| --- | --- | --- | --- | --- | --- |
| A <1年 | 一致 | 一致 | 需注意 | 衝突 | 衝突 |
| B 1～3年 | 需注意 | 需注意 | 一致 | 一致 | 一致 |
| C 3～5年 | 需注意 | 需注意 | 一致 | 一致 | 一致 |
| D 5～10年 | 衝突 | 衝突 | 需注意 | 一致 | 一致 |
| E ≥10年 | 衝突 | 衝突 | 需注意 | 一致 | 一致 |

「衝突」**不代表使用者答錯**，應解釋為「真正能長期承擔市場波動的資金，可能少於目前投入的金額」。

#### Finding 4：知識－商品經驗一致性（priority 4）

| Q12 | 商品經驗 | 結果 |
| --- | --- | --- |
| 正確（C） | 無經驗 | 基礎分散概念已理解 |
| 正確（C） | 一般商品 | 基礎分散概念已理解 |
| 正確（C） | 複雜商品 | 經驗與基本概念無明顯衝突 |
| 錯誤／不確定 | 無經驗 | 新手，採基礎解釋 |
| 錯誤／不確定 | 一般商品 | 需補充分散投資概念 |
| 錯誤／不確定 | 複雜商品 | 知識與商品經驗存在落差 |

**Q10 不參與此表**（D-21）。Q10 只用於資料一致性檢查。

#### 資料一致性檢查 → readiness

| 條件 | `issues[].kind` | `readiness` |
| --- | --- | --- |
| Q10 = A（無經驗）但 Q11 勾選了 B–I 任一項 | `experience_conflict` | `limited` |
| Q10 ∈ {D, E}（≥3年）但 Q11 只勾 K | `experience_conflict` | `limited` |
| 任一題未作答 | `missing_answer` | `blocked` |
| 其餘 | — | `ready` |

`limited` 時前端**擋住後續流程**，導回問卷並標示衝突的題號（D-17）。

### 4.2.4 模擬模式的重算範圍

`mode = simulation` 時，使用者可覆寫 `lossTolerance` 與 `investmentHorizon`（D-14）。後端以覆寫後的值**重算兩條 finding**：

| Finding | 是否重算 | 理由 |
| --- | :---: | --- |
| 主要財務限制 | **部分重算** | 只重算「風險意願 > 能力」與「Q7／Q8 衝突」兩個觸發條件，其餘沿用 |
| 風險意願－能力關係 | 重算 | 直接依賴 `lossTolerance` |
| 投資期限－流動性一致性 | 重算 | 直接依賴 `investmentHorizon` |
| 知識－商品經驗一致性 | 沿用 | 不依賴這兩個欄位 |

`financialCapacity` 與 `liquidityNeed` **永不重算**，因為它們的來源題目不開放覆寫。
模擬結果**不寫回** `risk_profiles`，只存在於該次 `analysis_results` 的 `changedFields` 與 `findings` 中。

---

## 4.3 錯誤處理

### 4.3.1 錯誤分類總表

| 類別 | 範例 | 處理策略 | 使用者看到的訊息 | 日誌層級 |
| --- | --- | --- | --- | --- |
| 使用者輸入錯誤 | 股數為負、日期在未來 | 回 400，不重試 | 明確指出哪個欄位、為什麼不合法、怎麼改 | `info` |
| 認證失效 | 通行證過期 | 回 401，前端導向登入頁 | 「請重新登入」 | `info` |
| 流程前置未完成 | 尚未填問卷 | 回 409，前端導向問卷頁 | 「請先完成風險評估問卷」 | `info` |
| 資料不足 | 無共同期間、基準缺資料 | 回 422，不重試 | 說明缺什麼、可以怎麼辦（例：「這檔股票上市未滿一年，請縮短分析期間或先移除」） | `info` |
| 超過上限 | 第 21 個投資組合 | 回 422 | 明確寫出上限數字 | `info` |
| 外部服務失敗 | 銀行網頁改版、Gemini 逾時 | 見 §4.3.2 | 「服務暫時無法使用」＋重試入口 | `warn` |
| 資料不一致 | 外鍵找不到對應的 `stock_info` | 立即中止，**不自動修復** | 通用錯誤訊息 ＋ `trace_id` | `error` |
| 程式錯誤 | 未捕捉的例外 | 回 500 | 通用錯誤訊息 ＋ `trace_id` | `error` |

### 4.3.2 外部服務的重試與降級

| 服務 | 重試 | 降級行為 |
| --- | --- | --- |
| Gemini（問卷描述） | 2 次，退避 1 秒 / 3 秒 | `description_status = failed`。四項核心指標與四條 finding **照常顯示**，只有那段文字缺席，並提供重試按鈕 |
| Gemini（分析報告） | 同上 | `analysis_reports.status = failed`。前端進入 `partial`：四張圖與全部數字照常顯示 |
| 銀行牌告網頁 | 後端每家重試 3 次（間隔 5、20 秒）；n8n 不重試 | 任一家最終失敗即整批失敗回 502，**不寫入任何資料**，舊值保留。後續分析仍可用舊的 $R_f$，報告標註 `rateAsOf` |
| yfinance 個股價格 | 每批重試 3 次；連續 3 批失敗即中止 | 已提交的批次保留，失敗代號記入 `failed` 並回 502；下次執行整段覆寫補回 |
| 證交所報酬指數 | 每月重試 3 次 | 某月最終失敗即停在該月之前，資料庫不出現缺口，回 502 |
| 資料庫 | 連線層 `pool_pre_ping` 自動重連 | 交易失敗即回滾，回 500 |

**只在 JSON 解析或 schema 驗證失敗時重試模型**。HTTP 4xx（金鑰錯誤、超過配額）**一律不重試**——重試只會更快耗盡配額。

### 4.3.3 治理原則

1. **使用者看到的訊息不得洩漏內部細節**：不含堆疊追蹤、SQL、檔案路徑、內部服務名稱、模型原始輸出。
2. **每個錯誤回應都帶 `trace_id`**，與日誌中的同一欄位對應。
3. **可重試與冪等**：`GET` 與 `DELETE` 天然冪等；`POST /market-data/fetch` 與 `POST /stocks/fetch` 以覆寫達成冪等；`POST /portfolios/{id}/analysis` **不冪等**（每次產生新快照），前端須以送出中狀態阻止重複點擊。
4. **資料不一致絕不自動修復**。例如某檔 `stock_info` 被刪但仍有 `holding_lots`（外鍵應已擋下），發生即代表有人繞過了約束，必須中止並記錄，不得猜測意圖。

### 4.3.4 空資料與邊界的畫面行為

| 情境 | 畫面 |
| --- | --- |
| 沒有任何投資組合 | 空狀態卡：說明一句話 ＋「新增投資組合」主要按鈕 |
| 投資組合沒有買進紀錄 | 空狀態卡 ＋「新增買進紀錄」按鈕；「開始分析」按鈕為 `disabled` 並附說明 |
| 只有一檔持股 | 正常顯示。熱圖區塊顯示「只有一檔持股，沒有可比較的配對」，**不隱藏該區塊** |
| 指標為 `unavailable` | 該卡片顯示「—」與 `reason`，**不顯示 0**，不隱藏卡片 |
| 實際期間 < 所選期間 | 報告頂部固定顯示 `periodNotice`，列出實際起訖日、交易日數與造成限縮的代號 |
| 風險屬性 `readiness` 為 `limited` | 導回問卷頁，衝突題目以 `error` 邊框標示，頂部顯示說明。其他頁面一律擋住 |
| 風險屬性 `readiness` 為 `blocked` | 同上，但說明文字為「問卷有題目未作答」。此為防禦性狀態，正常流程不會出現 |
| 某檔持股無最新報價 | 該檔市值與權重標為不可用，整個分析回 `INSUFFICIENT_PRICE_DATA`（權重必須加總為 1） |

---

## 4.4 介面規格

設計的唯一依據是 `DESIGN.md`。本節只定義**結構、狀態與行為**，不重複定義顏色、圓角、字級——那些一律查 token。

### 4.4.1 路由表

| 路徑 | 頁面元件 | 需登入 | 額外守衛 |
| --- | --- | :---: | --- |
| `/login` | `Login`（mode=login） | 否 | 已登入 → 導向 `/` |
| `/register` | `Login`（mode=register） | 否 | 同上 |
| `/questionnaire` | `Questionnaire` | 是 | — |
| `/risk-profile` | `RiskProfile` | 是 | 無風險屬性 → `/questionnaire` |
| `/` | `Home`（投資組合清單） | 是 | 同上 |
| `/portfolios/:portfolioId` | `PortfolioDetail` | 是 | 同上；非本人資源 → `/` |
| `/portfolios/:portfolioId/analysis/:analysisId` | `AnalysisReport` | 是 | 同上 |
| `/settings` | `Settings` | 是 | 同上 |
| `*` | 導向 `/` | — | — |

**Gating（FR-06）** 以 `GET /auth/me` 的 `hasRiskProfile` 判定，在 `RequireAuth` 之後、頁面渲染之前執行。`readiness = limited` 時同樣導回 `/questionnaire`，並以 query string 帶上衝突題號（例 `?conflict=Q10,Q11`），該頁將對應題目以 `error` 邊框標示。

### 4.4.2 各頁結構

| 頁面 | 主要區塊 | 空狀態 | 載入狀態 | 錯誤狀態 |
| --- | --- | --- | --- | --- |
| `Login` | 標題、帳密表單、切換連結 | — | 按鈕 `disabled` ＋ 文字改為「處理中」 | 表單下方 `error-container` chip |
| `Questionnaire` | 進度指示、分四部分的題組、送出按鈕 | — | 送出中：按鈕 `disabled` | 未填題目捲動定位並標示 |
| `RiskProfile` | 四張核心指標 stat tile、AI 描述卡、警示 chip、「建立投資組合」按鈕 | — | 描述卡骨架 | 描述失敗：卡內顯示說明 ＋ 重試按鈕 |
| `Home` | 投資組合卡片格線、新增按鈕 | 空狀態卡 | 卡片骨架 | 整頁錯誤卡 |
| `PortfolioDetail` | 總覽數字列、持股表格（可展開）、買進紀錄表單、「開始分析」按鈕、歷史分析清單 | 空狀態卡 | 表格骨架 | 整頁錯誤卡 |
| `AnalysisReport` | 期間提示、摘要、四張圖與圖說、六段解說、檢視方向、名詞說明、限制說明 | — | 見 §4.4.3 | 見 §4.4.3 |
| `Settings` | 帳號資訊、修改密碼表單、主題切換 | — | 按鈕 `disabled` | 表單下方 chip |

### 4.4.3 分析頁的五種狀態

| 狀態 | 畫面 | 是否顯示圖表 |
| --- | --- | :---: |
| `computing` | 置中載入指示器 ＋「正在計算量化指標」＋ 次行「約需數秒」 | 否 |
| `interpreting` | 同上，文字改為「正在產生分析解說」＋ 次行「約需 10 至 30 秒」 | 否 |
| `ready` | 完整報告一次揭露 | 是 |
| `partial` | 完整圖表與數字；解說區塊為 `warning-container` 卡片，內含失敗說明與「重新產生解說」按鈕 | 是 |
| `failed` | 錯誤卡片，依錯誤碼顯示對應說明與可行動作 | 否 |

載入指示器為 `primary` 色的環形進度動畫；`prefers-reduced-motion: reduce` 時改為靜態文字與脈動透明度（不旋轉）。

### 4.4.4 四張圖的共同規範

| 項目 | 規範 |
| --- | --- |
| 函式庫 | Nivo 0.99.0（P-29） |
| 主題 | 共用 `theme/nivoTheme.ts`，其值以 `getComputedStyle` 讀自 `styles/tokens.css`。**圖表檔案中不得出現任何色碼字面值** |
| 深色模式 | 深色是**獨立挑選的一組色**，不是淺色的自動反轉。主題切換時重新計算 `nivoTheme` 並重繪 |
| 軸線與格線 | 1px `outline`（深色用 `dark-outline`）；軸標籤 `label-md`、色 `on-surface-variant` |
| 容器 | 圖表置於 `surface` 卡片內，卡片圓角 30px，圖表自身圓角 `rounded.lg` |
| 互動 | **每張圖都必須有 hover 提示**。線圖為十字準線 ＋ 提示框，長條與格子為單一標的提示框。提示框為 `level-3` 玻璃、`rounded.xl`、文字 `body-sm` |
| 點擊目標 | 不小於 44×44 CSS 像素；格子過小時以透明覆蓋層擴大感應範圍 |
| 圖例 | 兩組以上序列必須有圖例；四組以內同時加上直接標籤。**識別不得僅靠顏色** |
| 替代內容 | 每張圖旁附一個視覺隱藏的 `<table>`，欄位與圖上資料一致，供螢幕閱讀器與列印使用（FR-34） |
| 數字格式 | 比例一律顯示到小數點後 2 位的百分比（`18.32%`）；相關係數顯示到小數點後 2 位 |

**分類色**

| 模式 | 序列 1 | 序列 2 | 來源 token |
| --- | --- | --- | --- |
| 淺色 | `#1e3a8a` | `#0d9488` | `primary-500`、`secondary-500` |
| 深色 | `#1e3a8a` | `#0d9488` | `primary-500`、`secondary-500` |

兩種模式使用同一組色（D-48），與 `DESIGN.md`「primary、secondary、tertiary 在深色模式維持相同 500 填色」的規則一致。

**色盲安全性**：`#1e3a8a` ↔ `#0d9488` 的 CVD 分離度 ΔE 25.7（deutan）、22.8（tritan）、27.2（正常視覺），三者皆遠高於 8 的目標值，兩色的識別性沒有問題。

**已知限制**：`#1e3a8a` 對 `dark-surface`（#000000）的對比約 1.68:1，填色在深色模式下與背景的分離較弱。本版接受此結果（D-48），不另加描邊或加粗。§4.4.4 既有的「兩組以上序列必須有圖例、四組以內加直接標籤」與每張圖的視覺隱藏表格替代（FR-34），已足以讓識別不依賴顏色。

淺色模式下 `#1e3a8a` 對 `surface` 的對比為 10.36:1，無此問題。

**發散色階（熱圖專用）**

| 位置 | 淺色 | 深色 |
| --- | --- | --- |
| $\rho=-1$ | `info` #2563eb | `dark-info` #2563eb |
| $\rho\approx-0.3$ | `info-container` #dfeaff | `dark-info-container` #00124d |
| $\rho=0$ | `surface` #ffffff | `dark-surface` #000000 |
| $\rho\approx+0.3$ | `error-container` #fae1e1 | `dark-error-container` #3e0b0b |
| $\rho=+1$ | `error` #dc2626 | `dark-error` #dc2626 |

中點為中性面色，兩極為兩個色相，符合發散色階的構成規則（不得在中點使用色相）。
`error` 在此代表「正相關」而非錯誤，屬 `SPEC.md` §0.3 登記的語意色例外，**僅限熱圖**。

### 4.4.5 圖一：相關係數熱圖

| 項目 | 規範 |
| --- | --- |
| 元件 | `@nivo/heatmap` |
| 資料 | `correlation.symbols` 與 `correlation.matrix` |
| 色階定義域 | **固定 `[-1, 1]`，`divergeAt: 0.5`**。不得隨資料自動縮放——自動縮放會讓「最紅」在不同組合間代表不同的相關程度 |
| 軸標籤 | 股票**代號**（名稱太長會撐破格線）。hover 提示框顯示名稱與代號 |
| 格內數值 | 一律標出，小數點後 2 位 |
| 格內文字色 | 依該格填色的相對亮度自動選 `on-surface` 或 `inverse-on-surface`，取對比 ≥ 4.5:1 者 |
| 對角線 | 填 `surface-container-high`、標「—」。**不得顯示 1.00**，避免被讀成「最高相關」 |
| 缺值 | 填 `outline-variant` ＋ 45° 斜線紋理，標「—」。**不得填白色**（白色代表 $\rho=0$） |
| 尺寸 | 不同股票 ≤ 20 檔：填滿容器寬度。> 20 檔：固定格邊長 32px，容器開啟雙向捲動 |
| 小格降級 | 格邊長 < 28px 時省略格內數值，改為 hover 顯示，並在圖說註明 |
| 圖例 | 連續色條，標 `−1`、`0`、`+1` 三個刻度與文字說明 |

### 4.4.6 圖二：權重與風險貢獻對照

| 項目 | 規範 |
| --- | --- |
| 元件 | `@nivo/bar`，水平、分組（非堆疊） |
| 序列 | 兩組：`權重`、`風險貢獻比例`。兩者皆為比例，**共用單一座標軸**（禁止雙軸） |
| 預設列數 | 依 `pcr` 由大到小取前 5 名（D-18）；「顯示全部」展開為全部持股 |
| 排序 | `pcr` 遞減。`pcr` 為 `null` 時排在最後 |
| 負值 | `pcr` 可為負。座標軸必須跨越 0，零線以 1px `outline` 標示 |
| 分組間距 | 同一標的的兩條長條之間留 2px 面色間隙 |
| 直接標籤 | 每條長條末端標百分比（`label-md`）。長條過短時標於外側 |
| 提示框 | 顯示代號、名稱、權重、PCR、兩者差值 |
| 展開提示 | 若有 `pcr < 0` 的持股未進入前 5 名，圖說追加一句「另有具抵銷效果的持股，請展開查看」 |
| 不可用 | `σp = 0` 導致全部 `pcr` 為 `null` 時，`status` 為 `unavailable`，`reason`「組合波動為零，無法拆解風險來源」 |

### 4.4.7 圖三：淨值走勢與回撤

**兩個上下堆疊、共用 x 軸的圖，不是一張雙軸圖。** 淨值指數與回撤的尺度不同，雙軸會製造任意的視覺交叉點。

| 面板 | 元件 | 內容 |
| --- | --- | --- |
| 上（高度佔 60%） | `@nivo/line` | 淨值指數 $V_t$，起點為 1。2px 線，序列 1 色，不填色 |
| 下（高度佔 40%） | `@nivo/line`，`enableArea` | 回撤 $DD_t$，範圍 $[MDD, 0]$。線用 `error`，面積用 `error-container`（深色用 `dark-error-container`），不透明 |

| 項目 | 規範 |
| --- | --- |
| x 軸 | 兩面板共用同一定義域與刻度；只有下面板顯示軸標籤 |
| MDD 標記 | 下面板的最低點加一個 ≥ 8px 的標記與直接標籤（例 `−28.37%`），並以 1px 虛線連到 x 軸標出日期 |
| 十字準線 | hover 時兩個面板同步顯示垂直準線；提示框顯示日期、淨值指數、當日回撤 |
| 刻度密度 | x 軸最多 8 個刻度，依期間長度自動選年或季；y 軸最多 5 個 |
| 不可用 | 有效日報酬 < 2 筆時 `status` 為 `unavailable` |

### 4.4.8 圖四：風險落差對照

Q13 給的是**區間**不是單點，因此這張圖是「區間 vs 單點」的比較，不是兩條長條。

| 元素 | 畫法 |
| --- | --- |
| 座標軸 | 水平，0% 至 $\max(35\%,\ \lvert MDD\rvert \times 1.15)$ |
| 可接受損失區間 | 橫跨該區間的色帶，填 `secondary-container`，上下緣各 1px `secondary`。兩端標出區間端點 |
| 開放區間 | 「未滿5%」自 0% 起；「30%以上」延伸至軸右端並在右端加箭頭符號與文字「以上」 |
| 實際最大回撤 | 自 0% 起的單一長條，序列 1 色，末端標 `−28.37%` |
| 超出判定 | 長條末端超過色帶右緣時，於圖下方顯示 `warning-container` chip：「這段歷史模擬的峰谷跌幅大於你填寫的區間」 |
| 尺度說明 | 圖說必須註明兩者的時間意義不同：Q13 是一年內的主觀意願，MDD 是整段分析期間的峰谷跌幅。**不得呈現為同尺度的機率比較** |
| 模擬模式 | `mode = simulation` 時，色帶旁加註「本次模擬」標籤 |

### 4.4.9 響應式規則

| 斷點 | 版面 |
| --- | --- |
| ≤ 734px | 單欄。持股表格改為卡片堆疊。四張圖各佔滿寬度，熱圖一律開啟捲動 |
| 735–1068px | 兩欄。核心指標 stat tile 兩欄排列 |
| ≥ 1069px | 三至四欄。內容欄寬約 1080px 置中 |

側邊留白：≥ 735px 為 32px、< 735px 為 16px。任何寬度下皆不得出現**頁面層級**的水平捲動；熱圖容器內部的捲動是允許的例外（FR-47）。

### 4.4.10 表單行為

| 行為 | 規範 |
| --- | --- |
| 買進紀錄的代號輸入 | 輸入 1 字即查詢 `GET /stocks?q=`，去抖 300ms，下拉顯示前 20 筆（代號 ＋ 名稱 ＋ 市場別）。**必須從清單選取**，不接受自由輸入 |
| 日期 | 原生 `<input type="date">`，`max` 設為今日 |
| 數值 | 原生 `<input type="number">`，`inputmode="decimal"`。失焦時格式化為 4 位小數 |
| 送出中 | 按鈕 `disabled` 且文字改為進行式，防止重複送出 |
| Q11 互斥 | 勾選 K 時自動取消其他選項；勾選其他任一項時自動取消 K |
| Q11 其他 | 勾選 J 時展開文字輸入框，`maxlength=100`，未填則送出按鈕 `disabled` |
| 分析前確認彈窗 | 滑桿（1–10 年，步進 1）＋ 兩個可調下拉 ＋ 兩個唯讀欄位。唯讀欄位以 `surface-container-highest` 底色與鎖頭圖示標示，並附一句說明「財務條件來自問卷，不可在此調整」 |

---

## 4.5 國際化與無障礙

| 項目 | 規範 |
| --- | --- |
| 語言 | 僅繁體中文（台灣）。`<html lang="zh-Hant-TW">`。多語系列為非目標 |
| 不翻譯的內容 | API 欄位名、錯誤碼、token 名稱、程式識別碼、股票代號 |
| 日期格式 | 顯示為 `2026/09/20`；區間顯示為 `2021/09/20 – 2026/09/19` |
| 數字格式 | 千分位逗號；百分比小數 2 位；金額小數 0 位（元）並標註幣別「元」 |
| 無障礙目標 | WCAG 2.2 AA |
| 對比 | 正文與 `on-surface-variant` 皆須達 4.5:1；圖表填色對面色須達 3:1（已於 §4.4.4 以工具驗證） |
| 鍵盤 | 所有互動元素可 Tab 聚焦，順序與視覺順序一致；焦點樣式為 2px `focus-ring` 外框加 1px 偏移；彈窗開啟時焦點鎖在彈窗內，`Esc` 關閉 |
| 圖表 | 每張圖有 `aria-label` 描述它在呈現什麼；資料以視覺隱藏的 `<table>` 提供；**不得僅以顏色編碼意義**（熱圖標數值、長條有直接標籤、回撤有文字標記） |
| 狀態提示 | 錯誤與成功訊息置於 `role="alert"` 容器；載入狀態使用 `aria-live="polite"` |
| 動效 | `prefers-reduced-motion: reduce` 時停用旋轉、位移與淡入動畫，保留透明度變化 |
| 強制色彩模式 | `forced-colors: active` 時，圖表的顏色編碼失效，因此熱圖的數值標籤與長條的直接標籤**不得隱藏** |

宣稱符合 WCAG 2.2 AA，因此 `spec/05-quality.md` §5.5 必須有對應的可執行驗收項；驗收項不存在的條款不得宣稱符合。
