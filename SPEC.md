# SPEC · 診股整股－投資組合量化風險分析平台

> 本檔為規格索引，包含協議層（§0）與產品層（§1）。系統層以下拆分於 `spec/`。
> 文件版本：1.6.0 ｜ 最後更新：2026-09-21 ｜ 狀態：草稿（draft）
> 對應程式版本：`main` 加 PR #8，再加問卷與風險屬性（階段 3）

---

# §0 協議層

## 0.1 文件權威

1. **本文件與 `spec/` 底下各子文件為單一事實來源（SSoT）。** 當本文件與現有程式碼、對話紀錄、截圖、口頭說明衝突時，以本文件為準。
2. 設計相關（顏色、字級、圓角、間距、元件、深色模式）的唯一依據是專案根目錄的 `DESIGN.md`。既有規格文件中的設計描述一律作廢。
3. 開發流程相關（worktree 測試、commit 署名、註解格式、changelog、README 維護、API 存取控制）的依據是 `CLAUDE.md`。本文件不重複其內容，僅在牴觸時以本文件為準並註明。
4. 決策來源分為兩類：`spec/00-cross-check.md` 記錄由柏鈞裁示的決策（D-xx）；`spec/01-presumed-decisions.md` 記錄由 AI 依業界標準預設、待覆核的決策（P-xx）。

## 0.2 Agent 工作規則

| 規則 | 內容 |
| --- | --- |
| 不得擴張範圍 | §1.5 列出的非目標一律不得實作，即使技術上很簡單 |
| 契約優先 | `spec/03-contract.md` 是資料契約。任何欄位新增、改名、移除，必須先改文件再改程式 |
| 遇到未涵蓋的情境 | 停止實作並詢問，不得自行決定。若必須當下決定，於 PR 說明中明確列出 |
| 驗收義務 | `spec/05-quality.md` §5.5 是驗收標準。實作後須逐項自我驗證，未全數通過不得視為完成 |
| 預設決策標記 | `P-xx` 為 AI 預設、尚未經柏鈞確認。照做即可，但不得在這些項目上再自行延伸 |
| 不得改設計 | 任何顏色、尺寸、圓角、字級一律查 `DESIGN.md` 的 token，不得沿用平台或框架預設值 |
| 不得改 Prompt | `spec/prompts/` 內的 Prompt 為規格。程式只能讀取，不得在程式碼中改寫、串接或追加指令句 |

## 0.3 詞彙表

**一個概念只有一個名字。** 下表未列出的概念不得自創命名。

| 詞彙 | 定義 | 程式識別碼 |
| --- | --- | --- |
| 使用者 | 已註冊並可登入的自然人帳號。不使用「會員」「用戶」 | `User` |
| 風險屬性 | 問卷作答經固定規則轉換後的結果快照，唯讀不可修改 | `RiskProfile` |
| 核心風險指標 | 前端顯示的 4 項：可接受損失區間、投資期限、資金流動性需求、財務風險承受能力 | `core_indicators` |
| 交叉分析結果 | 後端判定的關係與限制標籤，供 AI 解釋用 | `finding` |
| 資料問題 | 作答之間的衝突或缺漏，會影響 `readiness` | `issue` |
| 投資組合 | 使用者建立的一組持股集合 | `Portfolio` |
| 買進紀錄 | 一次買進行為：股票代號、買進日期、股數、每股價格。同一檔可有多筆。不使用「交易」「單」「批次」 | `HoldingLot` |
| 持股部位 | 同一檔股票所有買進紀錄的彙總結果，不落表、即時計算 | `position` |
| 加權平均成本 | 該檔全部買進紀錄的 Σ(股數×單價) ÷ Σ股數 | `average_cost` |
| 未實現損益 | 市值減投入成本，未納入手續費與交易稅 | `unrealized_pnl` |
| 市場基準 | 發行量加權股價報酬指數 IR0001（含息） | `market_benchmark` |
| 代號 | 有價證券代號，**純代號不含市場後綴**（`2330`，不是 `2330.TW`） | `symbol` |
| 股票基本資料 | 代號、名稱、市場別、產業別的對照資料。`IR0001` 亦收錄於此，`market` 為 `指數` | `StockInfo` |
| 分析 | 一次量化運算的唯讀快照，含當次採用的全部參數與結果 | `AnalysisResult` |
| 分析報告 | 針對某次分析由 AI 產生的解說，唯讀快照 | `AnalysisReport` |
| 分析期間 | 本次運算實際採用的交易日區間，可能短於使用者所選年數 | `effective_period` |
| 所選年數 | 使用者以滑桿選擇的年數，1～10 | `requested_years` |
| 無風險利率 | 五家公股銀行 1 年期定存機動利率的算術平均，取最新一批 | `risk_free_rate` |
| 最低可接受報酬率 | 下行波動度的門檻，本版等於無風險利率 | `mar` |
| 風險落差 | 使用者主觀可接受損失區間與投資組合實際最大回撤之間的差距 | `risk_gap` |
| 圖表參照 | AI 可引用的圖表白名單項目，格式 `figure:<name>` | `figure_ref` |
| 證據參照 | AI 可引用的資料白名單項目 | `evidence_ref` |

**語意色例外**：`DESIGN.md` 的 `error`（#dc2626）在相關係數熱圖中代表「正相關」而非錯誤，`info`（#2563eb）代表「負相關」。此例外**僅限熱圖**，其他任何畫面不得比照。

## 0.4 命名與語言慣例

完整表格見 `spec/01-presumed-decisions.md` §一。要點：Python `snake_case`、TypeScript `camelCase`、React 元件 `PascalCase`、資料表與欄位 `snake_case`（表名以複數為原則，但直白優先，例如 `stock_info` 不改為 `stock_infos`）、API 路徑 `kebab-case`、常數 `SCREAMING_SNAKE_CASE`。

程式碼註解、UI 文案、文件正文一律繁體中文（台灣）。變數名、函式名、檔名、API 路徑、資料表欄位、環境變數、錯誤碼、token 名稱一律英文，不得翻譯。

**時間欄位命名**：最後更新時間一律 `updated`；建立時間一律 `created`；業務日期各自具名（`trade_date`、`rate_as_of`）。唯讀快照表不設 `updated`。

**命名以直白為準**：資料表、欄位、API 路徑、檔名選最好懂的說法，不用行話與容易拼錯的複數變形。詳見 `spec/01-presumed-decisions.md` §〇。

## 0.5 ID 制度

| 前綴 | 意義 |
| --- | --- |
| `FR-xx` | 功能需求 |
| `NFR-xx` | 非功能需求 |
| `D-xx` | 已確認決策（柏鈞裁示），定義於 `spec/00-cross-check.md` |
| `P-xx` | AI 預設決策（待覆核），定義於 `spec/01-presumed-decisions.md` |
| `C-xx` | 外部限制（不可協商） |
| `R-xx` | 風險 |

ID 一經指派永不重用。需求移除時保留 ID 並標記 `（已移除，v<版本>）`。

## 0.6 TBD 清單

**目前為空。** 本文件無未決項目，可交付 Agent 實作。

日後若產生 `<TBD>` 標記，須登記於此表；**此表非空時，本規格不得交付實作**。

| 標記位置 | 需要的資訊 | 阻斷範圍 |
| --- | --- | --- |
| （無） | — | — |

---

# §1 產品層

## 1.1 一句話定位

**讓沒有量化背景的台股投資人，在建立自己的持股清單後，用四張圖與一段解說看懂「自己的投資組合風險，和自己實際能承受的風險，差在哪裡」。**

## 1.2 使用者與情境

| 項目 | 內容 |
| --- | --- |
| 主要使用者 | 有台股實際持股、但沒有量化或財務背景的個人投資人 |
| 專業程度 | 看得懂「漲跌幅」「定存利率」，但沒接觸過波動度、Beta、相關係數 |
| 使用情境 | 建立持股清單後主動查看；或市場大跌後想確認自己的組合承受度 |
| 使用頻率 | 每週 0～2 次，單次停留 5～15 分鐘 |
| 環境限制 | 桌機與手機瀏覽器，需連網；不支援離線 |
| 角色 | 僅一種角色（一般使用者）。本版無管理員角色，無權限矩陣 |

自動化服務 n8n 不是使用者角色，是以 `X-API-Key` 認證的機器呼叫端。

## 1.3 成功指標

| 指標 | 目標 | 量測方式 | 可達成性 |
| --- | --- | --- | --- |
| 首次完成「註冊 → 問卷 → 建組合 → 看到報告」 | ≤ 15 分鐘 | 使用者測試計時，5 人中位數 | 需問卷維持 14 題不增加 |
| 量化分析 API p95 回應時間（10 檔持股、5 年期間） | ≤ 3 秒（不含 AI 呼叫） | 後端計時日誌 | 數學上可保證，見 `spec/05-quality.md` §5.2 |
| AI 報告輸出通過 JSON schema 驗證 | 100% | 自動化測試：50 組固定輸入 × 3 次 | **無法保證模型輸出**，靠 schema 驗證 + 重試 + 降級達成，見 R-01 |
| 量化指標與黃金測試向量的相對誤差 | < 1e-6 | 單元測試 | 數學上可保證 |
| 報告中每個正式名詞都有白話說明 | 100% | 自動化檢查 `glossary` 覆蓋率 | 由 Prompt 契約保證，見 `spec/prompts/02_portfolio_system_prompt.md` |

## 1.4 功能需求

### 帳號（已部分實作）

| ID | 需求 | 優先 | 驗收 |
| --- | --- | :---: | --- |
| FR-01 | 使用者可用帳號與密碼註冊，註冊成功後自動登入 | Must | A1–A3 |
| FR-02 | 使用者可用帳號與密碼登入 | Must | A4–A5 |
| FR-03 | 使用者可登出，登出後原通行證失效 | Must | A6 |
| FR-04 | 使用者可查詢目前登入的帳號 | Must | A7 |
| FR-05 | 使用者可修改密碼，修改後其他裝置的登入全部失效 | Must | A8–A9 |

### 問卷與風險屬性

| ID | 需求 | 優先 | 驗收 |
| --- | --- | :---: | --- |
| FR-06 | 登入後若查無風險屬性紀錄，系統強制導向問卷頁，不得進入其他頁面 | Must | B1 |
| FR-07 | 使用者可填寫 14 題風險評估問卷並送出 | Must | B2–B3 |
| FR-08 | 系統依固定規則將作答轉換為 4 項核心風險指標 | Must | B4–B7 |
| FR-09 | 系統依固定規則產生 4 項交叉分析結果與主要財務限制標籤 | Must | B8–B11 |
| FR-10 | 系統呼叫 AI 產生一段客製化風險屬性描述 | Must | B12–B14 |
| FR-11 | 使用者可隨時重填問卷，每次重填產生新的風險屬性快照，舊快照保留 | Must | B15 |
| FR-12 | 作答存在資料衝突時，系統擋住流程並導回問卷標示衝突題號 | Must | B16 |

### 投資組合

| ID | 需求 | 優先 | 驗收 |
| --- | --- | :---: | --- |
| FR-13 | 使用者可建立投資組合並命名 | Must | C1 |
| FR-14 | 使用者可新增一筆買進紀錄，輸入股票代號、買進日期、股數、每股價格 | Must | C2–C4 |
| FR-15 | 同一檔股票可有多筆買進紀錄，日期、股數、單價各自獨立 | Must | C5 |
| FR-16 | 使用者可修改與刪除單筆買進紀錄，可刪除整個投資組合 | Must | C6–C7 |
| FR-17 | 持股列表預設每檔一列，顯示加權平均成本、部位股數、市值、未實現損益 | Must | C8–C9 |
| FR-18 | 持股列表可展開檢視該檔的全部買進紀錄，含每筆的日期、股數、單價、該筆損益與持有天數 | Must | C10 |
| FR-19 | 系統依實際持有天數計算年化持有報酬率；持有天數未滿 30 日時不年化並標示原因 | Must | C11–C12 |
| FR-20 | 凡顯示損益處固定標註「未納入手續費與交易稅」 | Must | C13 |
| FR-21 | 使用者輸入股票代號時可搜尋代號或名稱 | Should | C14 |

### 量化分析

| ID | 需求 | 優先 | 驗收 |
| --- | --- | :---: | --- |
| FR-22 | 分析前顯示確認彈窗，使用者可調整分析期間滑桿（1～10 年，預設 5 年） | Must | D1 |
| FR-23 | 確認彈窗中使用者可單次調整可接受損失區間與投資期限，不寫回資料庫 | Must | D2–D3 |
| FR-24 | 確認彈窗中財務風險承受能力與資金流動性需求為唯讀，不可調整 | Must | D4 |
| FR-25 | 後端計算 11 項量化指標、每檔持股的風險貢獻度與完整相關係數矩陣 | Must | D5–D15 |
| FR-26 | 量化指標一律以目前市值權重套用歷史報酬，買進日期不影響指標數值 | Must | D16 |
| FR-27 | 所選期間內有持股資料不足時，系統自動限縮至最長共同期間 | Must | D17 |
| FR-28 | 實際分析期間短於所選年數時，報告顯著處說明實際起訖日與交易日數 | Must | D18 |
| FR-29 | 每次分析產生唯讀快照，內含當次全部參數與結果 | Must | D19 |

### 分析報告

| ID | 需求 | 優先 | 驗收 |
| --- | --- | :---: | --- |
| FR-30 | 報告頁呈現相關係數熱圖，每格標出數值，色階定義域固定為 −1 至 +1 | Must | E1–E3 |
| FR-31 | 報告頁呈現權重與風險貢獻對照圖，主畫面顯示前 5 名，其餘收在「顯示全部」後 | Must | E4–E5 |
| FR-32 | 報告頁呈現組合淨值走勢與回撤面積圖 | Must | E6 |
| FR-33 | 報告頁呈現可接受損失區間與實際最大回撤的對照條 | Must | E7 |
| FR-34 | 四張圖皆提供螢幕閱讀器可讀的資料表替代內容 | Must | E8 |
| FR-35 | 系統呼叫 AI 產生分析解說，含摘要、六個固定段落、四張圖的圖說與名詞白話層 | Must | E9–E14 |
| FR-36 | 分析等待期間顯示分階段狀態（量化指標計算中／分析解說產生中），期間不顯示任何圖表 | Must | E15–E16 |
| FR-36a | AI 呼叫失敗時，量化結果與四張圖照常顯示，解說區塊顯示失敗說明與重試入口 | Must | E17 |
| FR-37 | 使用者可回看同一投資組合的歷史分析報告 | Must | E18 |
| FR-38 | 報告頁顯示持股的未實現損益供使用者對照，但成本與損益資料不送入 AI | Must | E19–E20 |

### 自動化與資料管線

| ID | 需求 | 優先 | 驗收 |
| --- | --- | :---: | --- |
| FR-39 | n8n 可呼叫後端抓取五家公股銀行 1 年期定存機動利率並寫入資料庫 | Must | F1–F3 |
| FR-40 | n8n 可呼叫後端抓取台股日收盤價並寫入資料庫；首次抓 10 年，之後只抓近 1 個月；同一代號同一日期重抓直接覆寫，不產生重複列；抓取中斷不得留下半截資料 | Must | F4–F6、F11、F12、F14、F15 |
| FR-41 | 同一支抓取端點同時處理個股與市場基準 IR0001 的收盤價，回傳中分別列出個股與大盤的成功筆數 | Must | F7 |
| FR-41a | n8n 可呼叫後端抓取並同步個股基本資料（代號、名稱、市場別、產業別），只新增與覆寫、不刪除 | Must | F7a |
| FR-42 | 每日股價抓取全部成功後，後端自動清理超出保留期的歷史報價 | Should | F8 |
| FR-43 | 所有僅供 n8n 呼叫的端點必須驗證 `X-API-Key`，未設定金鑰時一律拒絕；成功與失敗回傳同一層的統一欄位（不含時間） | Must | F9、F10、F13、F16 |

### 介面

| ID | 需求 | 優先 | 驗收 |
| --- | --- | :---: | --- |
| FR-44 | 全站樣式依 `DESIGN.md` 的 token 實作，不使用框架預設值 | Must | G1–G3 |
| FR-45 | 全站支援淺色與深色模式，跟隨系統設定並可手動切換；切換時四張圖的配色同步更新 | Must | G4 |
| FR-46 | 每個畫面定義空狀態、載入中狀態與錯誤狀態 | Must | G5–G7 |
| FR-47 | 版面在 320px 至 2560px 之間可用，不出現水平頁面捲動（熱圖的內部捲動除外） | Must | G8 |

### 非功能需求

| ID | 需求 | 依據 | 驗收 |
| --- | --- | --- | --- |
| NFR-01 | 量化分析 API p95 ≤ 3 秒（10 檔、5 年） | `spec/05-quality.md` §5.2 | D23 |
| NFR-02 | 量化分析最壞情況 ≤ 8 秒（50 檔、10 年） | 同上 | D23 |
| NFR-03 | 一般 CRUD API p95 ≤ 200 ms | 同上 | — |
| NFR-04 | 首頁 LCP ≤ 2.5 秒（Fast 4G，3 次中位數） | 同上 | — |
| NFR-05 | 前端 JS 總量 ≤ 500 KB（gzip） | 同上 | — |
| NFR-06 | 資料庫查詢命中索引，無 Seq Scan | 同上 | D24 |
| NFR-07 | 密碼以 Argon2id 雜湊，資料庫無明文；回應時間不洩漏帳號是否存在 | §5.1.1 | A3、A5 |
| NFR-08 | 每一支帶資源 id 的端點都在 service 層驗證所有權 | §5.1.2 | A11 |
| NFR-09 | 登入與分析端點限流 | §5.1.8 | A10 |
| NFR-10 | 機密不進版控、不進日誌、不進 `README.md` | §5.1.5 | — |
| NFR-11 | AI 相關的注入防護、資料外洩防護與成本上限 | §5.1.9 | E20–E22 |
| NFR-12 | 量化指標與黃金向量的相對誤差 < 1e-6 | §5.6 | D5–D15 |
| NFR-13 | 介面符合 WCAG 2.2 AA | `spec/04-behavior.md` §4.5 | G9–G14 |
| NFR-14 | 價格一律為還原權息值；金額欄位不得使用浮點型別 | §3.2、§4.1 | H1–H5 |

## 1.5 非目標

**下列項目一律不得實作，即使技術上很簡單：**

- **壓力測試**（任何形式的情境模擬、大盤衝擊推估、歷史情境重演）。最大回撤與 95% 預期短缺已承擔「最壞情境」的說明職能。
- **手續費與交易稅的計算**。損益一律以「每股價格 × 股數」為成本，畫面固定標註未納入。
- **賣出紀錄與已實現損益**。資料模型只記買進；減碼由使用者直接修改或刪除該筆買進紀錄達成。
- **時間加權報酬率 TWR 與內部報酬率 IRR**。本版的實際績效只做「持有天數」與「年化持有報酬率」。
- **以買進日期為基礎的動態權重回測**。14 項風險指標一律採固定目前市值權重的歷史模擬，這是刻意的口徑選擇。
- **任何形式的交易功能**：下單、模擬下單、券商串接、庫存同步。
- **投資建議**：個股推薦、ETF 推薦、買賣時機、加減碼比例、停損點、目標價。AI 的輸出契約已明文禁止。
- **四張指定圖表以外的圖**：日報酬分布直方圖、權重占比圓餅圖、指標量表卡、Sharpe／Sortino 對照條，皆列為非目標。
- **密碼強度政策與帳號鎖定**（D-19）。本版維持 1～128 字、僅英數、無鎖定。上線前必須重新評估，見 R-02。
- **多語系介面**。僅繁體中文（台灣）。
- **社群登入、頭像上傳、電子郵件驗證、忘記密碼**。
- **即時或盤中報價**。所有價格為日收盤的調整後價格。
- **台股以外的標的**：美股、港股、加密貨幣、期貨、選擇權、基金、債券。
- **多幣別**。僅新臺幣。
- **原生行動 App**。僅響應式網頁。
- **任何形式的使用行為追蹤或遙測**。
- **問卷結果的總分、風險人格綽號、固定風險分級**（如「積極型」「穩健型」）。規則文件已明確禁止。

## 1.6 決策紀錄

### 已確認決策

完整清單見 `spec/00-cross-check.md` §九～§十二（D-01 至 D-21）。影響最深的五項：

| # | 決策 | 結果 | 理由與取捨 |
| --- | --- | --- | --- |
| D-01 / D-07 | 分析期間 | 使用者以滑桿選 1～10 年，預設 5 年 | 讓使用者自行決定時間尺度；取捨是所有指標都變成「依所選期間計算」，跨使用者不可直接比較 |
| D-03 / D-06 / D-08 | 無風險利率 | 五家公股銀行 1 年期定存機動利率的算術平均，取最新一批 | 推翻原指標說明書的 $R_f=0\%$。取捨是同一組合在不同日期分析會得到不同 Sharpe，屬預期行為，報告須標註採用值與抓取日 |
| D-10 | 最低可接受報酬率 | $MAR = R_f$，下行波動度改為 $\min(R_t - R_{f,daily},0)$ | 讓 Sharpe 與 Sortino 同基準，偏態決定解讀順序的設計才成立。取捨是與原指標說明書的 $\min(R_t,0)$ 公式不相容，舊數值作廢 |
| D-14 | 分析前微調範圍 | 可調意願類（損失區間、投資期限）與分析期間；財務承受能力與流動性需求鎖死 | 對應 CFA 的 willingness／capacity 區分。取捨是後端須為模擬情境重算兩條 finding |
| D-16 / D-22 | 買入成本 | 改為交易明細：同一檔可多筆買進，每筆含日期、股數、每股價格。顯示加權平均成本與未實現損益，但不送入 AI | 避免 AI 把「目前獲利」誤讀成「風險低」。取捨是交接書提到的「AI 行為診斷」取消 |
| D-23 | 市場基準 | `IR0001` 發行量加權股價報酬指數（含息） | 與個股採用的 `adj_close`（已還原除權息）基準一致。若改用價格指數 `IX0001`，個股含息而指數不含息，Beta 會有偏誤、超額報酬會被系統性高估約 3～4%／年 |
| D-25 | 圖表函式庫 | Nivo | 唯一同時具備原生熱圖、React 原生、theme 可餵 token 的方案。取捨是體積比 Recharts 大約 60KB |

### AI 預設決策

P-01 至 P-38，完整清單見 `spec/01-presumed-decisions.md`。**這些項目已寫死於各層文件，但尚未經柏鈞逐項確認。** 覆核時只需回報要修改的編號。

## 1.7 外部限制

| # | 限制 |
| --- | --- |
| C-01 | 台股日報價來自 yfinance，市場基準 `IR0001` 來自臺灣證券交易所。兩者由後端抓取服務（`services/market_data.py`）執行並直接寫入資料庫，n8n 只負責啟動與顯示結果（D-02、D-23、D-56） |
| C-02 | 五家公股銀行的利率以網頁爬取取得，受各行網站改版影響 |
| C-03 | 開發環境以 Docker Compose 為唯一啟動方式，連接埠固定為預設值 +1（見 `README.md`） |
| C-04 | 必須遵守 `CLAUDE.md` 的 commit 署名規則：commit 訊息與 PR 說明**不得**加入任何 AI 署名 |
| C-05 | `README.md` 不得寫入 API 路徑、HTTP 方法、金鑰值與內部網址（`CLAUDE.md` §7） |

## 1.8 風險

| # | 風險 | 影響 | 緩解 |
| --- | --- | --- | --- |
| R-01 | `gemini-3.5-flash` 是滾動別名，Google 未發布帶日期的 snapshot，模型行為可能隨時間漂移 | AI 解說的措辭與結構可能改變 | 驗收項一律以「符合 JSON schema」為準，不以「內容正確」為準；輸出解析失敗時重試並降級（P-38） |
| R-02 | 密碼規則維持 1 字元、僅英數、無鎖定（D-19） | 帳號可被暴力破解 | 本版限定開發階段使用。登入端點需限流（P-04）。上線前必須重新評估，並於 `README.md` 標註 |
| R-03 | yfinance 非官方 API，可能中斷或改變回傳格式 | 日報價管線失效，分析無法執行 | 抓取邏輯集中於單一服務，可替換來源；失敗的批次自動重試、連續失敗即中止並回報，已寫入的資料不受影響；缺資料時回 `INSUFFICIENT_PRICE_DATA` 而非崩潰 |
| R-04 | 五家銀行網頁改版導致爬蟲失效 | 無風險利率取不到，Sharpe 與 Sortino 無法計算 | `risk_free_rate` 取「最近一批完整五家」並標註日期（P-14）；完全無資料時回 `RISK_FREE_RATE_UNAVAILABLE`，其餘指標照常輸出 |
| R-05 | AI 呼叫成本隨使用量成長 | 費用失控 | 每次分析硬性限制輸出 token（`GEMINI_MAX_OUTPUT_TOKENS`）、重試上限 2 次、問卷送出每使用者每分鐘 1 次限流（分析端點不另設限流）、結果快取 24 小時 |
| R-06 | 相關係數矩陣在持股數多時為 $O(N^2)$ | 50 檔持股產生 1225 組配對，前端渲染與 payload 膨脹 | 不同股票上限 50 檔（P-22）；矩陣以下三角傳遞；熱圖超過 20 檔時前端改為固定格子邊長並可捲動（P-43） |
| R-07 | 字型改走 Google Fonts CDN（P-28） | 外部相依：CDN 不可用時字型退回系統預設；需為 `fonts.googleapis.com` 與 `fonts.gstatic.com` 開 CSP 例外 | `font-display: swap` 確保文字不空白；`tokens.css` 的 `font-family` 保留完整 fallback 鏈（Noto Sans TC → 系統無襯線字型）；CSP 例外明列於 `spec/05-quality.md` §5.1 |
| R-08 | `IR0001` 的來源端點一次只回傳一個月 | 首次回補 10 年需呼叫證交所約 120 次，過快可能被限流；日常也需每日補近期月份 | 由後端逐月抓取並加間隔（2 秒）、每月獨立重試；某月最終失敗就停在該月之前，資料庫不出現缺口；後端在基準資料不足時回 `BENCHMARK_UNAVAILABLE`，Beta 與 R² 標為不可用，其餘 9 項指標照常輸出 |

---

# 文件導航

| 檔案 | 內容 | 何時必讀 |
| --- | --- | --- |
| `SPEC.md`（本檔） | 協議層、產品層、需求清單、非目標、決策與風險 | 任何實作之前 |
| `DESIGN.md` | 設計系統：色票、字級、圓角、間距、元件、深色模式 | 動到任何畫面之前 |
| `CLAUDE.md` | 開發流程：worktree 測試、commit 規範、註解格式、changelog、README 維護、API 存取控制 | 開始寫程式之前、開 PR 之前 |
| `spec/00-cross-check.md` | 原始文件間的不一致、規格缺口、D-01～D-21 決策紀錄 | 想知道「為什麼是這樣決定的」 |
| `spec/01-presumed-decisions.md` | P-01～P-38：命名慣例、API 路由、資料表欄位、量化參數、環境變數、樣式導入方式 | 任何實作之前 |
| `spec/02-system.md` | 技術棧版本鎖定、執行環境、架構與資料流、檔案結構、模組相依規則、環境變數 | 任何實作之前 |
| `spec/03-contract.md` | 資料模型、資料庫 schema、API 契約、狀態與事件、外部整合、模型與 Prompt 契約 | 動到資料結構或 API 時 |
| `spec/04-behavior.md` | 量化演算法、問卷轉換規則、業務規則與驗證、錯誤處理、依 `DESIGN.md` 的頁面規格 | 實作功能邏輯時 |
| `spec/05-quality.md` | 資安、效能預算、可觀測性、測試策略、驗收清單、黃金測試向量 | 資安與效能在實作前讀；驗收清單在實作後讀 |
| `spec/06-execution.md` | 階段拆分、完成定義、版控規範、部署與回滾 | 開工前與交付前 |
| `spec/prompts/01_profile_system_prompt.md` | 問卷結果解說的 System Prompt | 實作問卷階段 AI 呼叫時 |
| `spec/prompts/01_profile_user_prompt.md` | 問卷結果解說的 User Prompt 模板 | 同上 |
| `spec/prompts/02_portfolio_system_prompt.md` | 分析報告的 System Prompt | 實作分析階段 AI 呼叫時 |
| `spec/prompts/03_user_prompt_template.md` | 分析報告的 User Prompt 模板與圖表參照命名 | 同上 |
| `spec/appendix/B-constants.md` | 錯誤碼、指標識別碼、CSS 變數名稱完整清單 | 需要查名稱時 |
| `spec/appendix/C-fixtures.md` | 黃金測試向量的定義、數值與產生方式 | 實作演算法與寫測試時 |
| `tests/fixtures/` | 參考實作、向量產生腳本、`golden_vectors.json` | 同上 |
| `spec/appendix/D-references.md` | **資料來源與參考文獻**：每條公式、每條規則的出處，以及哪些是本專題自訂 | 撰寫專題書面、被問「這是哪來的」時 |
| `spec/appendix/E-changelog.md` | 本規格的版本紀錄 | 每次改規格時 |

**最小閱讀集**：`SPEC.md` + `spec/01-presumed-decisions.md` + `spec/02-system.md` + `spec/05-quality.md` §5.5。其餘依任務載入。

## 任務對照表

| 任務 | 需讀 |
| --- | --- |
| 新增一支 API 端點 | `spec/03-contract.md` 全部、`spec/04-behavior.md` §4.2–4.3、`spec/05-quality.md` §5.1 |
| 實作量化指標 | `spec/04-behavior.md` §4.1、`spec/appendix/C-fixtures.md`、`spec/05-quality.md` §5.5 |
| 實作問卷轉換規則 | `spec/04-behavior.md` §4.2、`spec/03-contract.md` §3.1、`spec/appendix/C-fixtures.md` |
| 修改資料表 | `spec/03-contract.md` §3.1–3.2、`spec/06-execution.md` §6.4 |
| 做任何畫面 | `DESIGN.md` 全部、`spec/04-behavior.md` §4.4、`spec/01-presumed-decisions.md` §七 |
| 做圖表 | `DESIGN.md` §Charts、`spec/04-behavior.md` §4.4、`spec/prompts/03_user_prompt_template.md` |
| 串接 AI | `spec/03-contract.md` §3.11、`spec/prompts/` 全部、`spec/05-quality.md` §5.9 |
| 設定 n8n 排程 | `spec/03-contract.md` §3.5、`spec/01-presumed-decisions.md` P-32、`README.md` |
| 開 PR | `CLAUDE.md` §2、§4、§5、`spec/06-execution.md` §6.2–6.3 |
| 寫專題書面、準備口試 | `spec/appendix/D-references.md` 全部 |
