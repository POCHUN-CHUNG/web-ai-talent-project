# 013 · 銀行利率存入資料庫

- **Commit**：`5061aa0`　**日期**：2026-09-20　**作者**：POCHUN-CHUNG
- **一句話總結**：抓到的五大公股銀行利率現在會存進資料庫（只保留最新一批），並統一給 n8n 的回傳格式。

## 白話說明

以前抓完利率只是回傳給 n8n，沒有留存。現在每次抓取成功就覆寫資料庫中唯一的一列（五家利率＋更新時間）；任何一家失敗就整批不寫入、舊資料保留。回傳格式統一為「執行狀態（成功／失敗）＋訊息＋利率清單」。

## 變更檔案

| 檔案 | 異動 | 功能 |
| ---- | ---- | ---- |
| `backend/app/models.py` | 修改 | 新增「銀行利率」資料表，永遠只有一列。 |
| `backend/app/routers/bank_rates.py` | 修改 | 抓取後存入資料庫，失敗時保留舊資料並回報 502／500，回傳格式改為清單。 |
| `CLAUDE.md` | 修改 | 新增「n8n 排程端點的職責規則」：排程與時間由 n8n 決定，後端只執行、回傳、回報狀態。 |
